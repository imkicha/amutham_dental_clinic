// Root entry point for hosts that start the app with `node server.js`
// from the repository root (e.g. Hostinger's Express preset).
// The real server lives in server/server.js; this simply launches it.
require('./server/server.js');
