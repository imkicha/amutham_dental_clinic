{
  "name": "amutham-dental-clinic",
  "version": "1.0.0",
  "private": true,
  "description": "Amutham Dental Care — Express API serving the built React client (single Node app).",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install:server": "npm install --prefix server",
    "install:client": "npm install --prefix client",
    "build": "npm run install:client && npm run build --prefix client",
    "postinstall": "npm run install:server && npm run build",
    "start": "node server/server.js",
    "seed:admin": "npm run seed:admin --prefix server"
  }
}
